LAMP - linux apache MySQL php
запрос обрабатывает апач - приемник
php интерпретирует код и передает в базу
база - ответ - клиент

psr стандарт написания

var_dump($d) выводит тип переменной 

int - integer - целое число 
(int)($string) приводит к числу



СИНТАКСИС
"" - ``внутри двойных ковычек можно помещать переменные 
' ' - внутри одинарных ковычек переменные не определяются 
. - конкатенация echo 100.500 //100500 (string) // $fakenumber .= "прибавить строку";
echo $var."\n" - пробел
exit(), die() - останавливает ваыполнение когда ниже объявления этой функции

$_GET суперглобальный массив get - отправка в адресную строку

ПЕРЕМЕННЫЕ
    unset($var1, $var2 ... ) - удаляет переменные


[REQUIRE_INCLUDE] подключение одного файла php в другой (можно использовать создав отдельный файл function вынести туда все функции)
    require 'somefile.php'; // скрипт останавливается при ошибке в подключении
        если подключить два раза один и тот же файл то возникают проблемы (напр в        функциях)
        require_once - проверяет на множественное подключение
    include 'somefile.php'; // скрипт пролдолжает раюотать если неверно указано имя
        include_once


MASSIVES
    $a = array(1, 2, 3);
    $a = [1, 2, 3];
    $a[0] = 1;

    echo "<pre>";
    print_r($arr);  -- для вывода массива красиво
    echo "</pre>";

    unset($a[i]); - удаляет i элемент массива
    -Методы:
        foreach ($array as $key => $value) {}
        in_array("hello", $a) // true/false - ищет "hello" в массиве $a;

РАБОТА С ПАПКАМИ





